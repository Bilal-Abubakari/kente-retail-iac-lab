# Control-node dependencies. Install inside WSL (Ansible does not run natively on
# Windows). ansible-core brings ansible-playbook and ansible-vault; ansible-lint
# backs the pre-commit hook / value-add feature. Verified against ansible-core
# 2.20.x + ansible-lint 26.x (the versions in the current WSL toolchain); the
# floor is the oldest release the role's syntax is known to run on.
ansible-core>=2.16
ansible-lint>=24.0
