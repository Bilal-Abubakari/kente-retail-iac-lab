.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
# Exception: this one is deliberately checked in -- see monitoring.tf and
# the README for why. Everything else's state stays out of git as normal.
!legacy-networking-state/terraform.tfstate
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
*.pem
*.ppk
